<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.jalepo.cobalt.MenuActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/photos_button"
        android:layout_marginTop="50dp"
        android:layout_alignParentEnd="true"
        android:text="@string/photos"
        android:onClick="photosButtonClicked"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/videos_button"
        android:layout_alignParentEnd="true"
        android:text="@string/videos"
        android:layout_below="@id/photos_button"
        android:onClick="videosButtonClicked"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/videos_button"
        android:id="@+id/logout_button"
        android:layout_alignParentEnd="true"
        android:text="@string/logout"
        android:onClick="logoutButtonClicked"/>


</RelativeLayout>
